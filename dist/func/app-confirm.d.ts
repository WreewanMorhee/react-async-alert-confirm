export declare const app_confirm: ({ title, content, cancel_text, confirm_text, node_name, }: {
    title?: string;
    content?: string;
    cancel_text?: string;
    confirm_text?: string;
    node_name?: string;
}) => Promise<boolean>;
